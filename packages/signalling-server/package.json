{
  "name": "signalling-server",
  "version": "0.1.0",
  "description": "WebRTC signalling server for react-p2p",
  "license": "MIT",
  "author": "Peter Dodd",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --sourcemap --clean",
    "dev": "tsup src/index.ts --format cjs --dts --sourcemap --watch",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@types/ws": "^8.5.9",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
