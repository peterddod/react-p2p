{
  "name": "react-p2p-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "P2P state management for React - monorepo",
  "license": "MIT",
  "author": "Peter Dodd",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.1.25",
  "scripts": {
    "dev": "bun run --parallel dev --filter='./packages/{signalling-server,example}'",
    "dev:server": "bun run dev --filter=signalling-server",
    "dev:example": "bun run dev --filter=example",
    "build": "bun run build --filter='./packages/*'",
    "build:lib": "bun run build --filter=react-p2p",
    "build:server": "bun run build --filter=signalling-server",
    "build:example": "bun run build --filter=example",
    "start:server": "bun run start --filter=signalling-server",
    "test": "echo \"No tests specified\""
  }
}
